# Dependency pins for the Windows 7 compatible build.
# Python 3.8 is the last CPython that runs on Win7, which forces:
#   Flask <3.1 (needs py3.9), SQLAlchemy pinned for cp38 wheels,
#   watchdog <5 (needs py3.9), PyInstaller 5.x (6.x needs newer toolchain).
Flask==2.3.3
Werkzeug==3.0.6
Jinja2==3.1.4
Flask-Login==0.6.3
Flask-Session==0.5.0
Flask-SQLAlchemy==3.0.5
SQLAlchemy==2.0.23
Flask-Migrate==4.0.7
alembic==1.13.2
Markdown==3.7
PyYAML==6.0.2
python-dotenv==1.0.1
watchdog==4.0.1
flask-cors==5.0.1
